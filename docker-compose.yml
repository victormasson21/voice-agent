services:
  agent:
    build: ./agent
    env_file: frontend/.env.local
    restart: unless-stopped

  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    env_file: frontend/.env.local
    restart: unless-stopped
    depends_on:
      - agent